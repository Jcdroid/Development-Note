1. 已知圆心，半径，角度，求圆上的点坐标

	```
	圆点坐标：(x0,y0)
	半径：r
	角度：a

	则圆上任一点为：（x1,y1） 
	x1 = x0 + r * cos(a * 2π / 360) 
	y1 = y0 + r * sin(a * 2π / 360) 
	即
	x1 = x0 + r * cos(a * π / 180) 
	y1 = y0 + r * sin(a * π / 180) 
	```